const DUMMY_DATdjhfbmasA = [{}
]
<React />
<React />
<React />
<React />
<React />
<React />
<React />
<React />
const React = () => {
    
{if(DUMMY_DATA.length === 0) {<div></div>}else{DUMMY_DATA.map(item => <div>
    <h1>{item.name}</h1>
    <h4>{item.price}</h4>
</div>)
}}

};
export default React;

